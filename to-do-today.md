# in the ui stuff:
- [ ]
- [ v ] when shrink mode is true make the poppover when one of the file is active is false
- [ ] 

# in the backend stuff:
- [ ] auto save is active when the user is closed without a save in the file
- [ ] made the github model is not call manually, instaed call from resource it self 


# refactor
- [ ] move the type in the lib/constant.ts (read the midday ai code base first)
- [ ]
-


# doubt

- in the mathend/app/page.tsx we erase erase the note-card-active:
className={
  note.id === selectedNote?.id
    ? "note-card note-card-active note-card-button"
    : "note-card note-card-button"
}

-
